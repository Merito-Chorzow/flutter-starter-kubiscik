name: geo_journal
description: "Geo Journal – aplikacja Flutter z GPS i API"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  
  cupertino_icons: ^1.0.8

 
  geolocator: ^11.1.0

  
  geocoding: ^3.0.0

  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
